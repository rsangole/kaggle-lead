version: "3.9"
services:
  kaggle:
    image: "$DOCKER_DEVELOPMENT_IMAGE"
    ports:
      - "8900:8787"
      - "3900:3838"
    environment:
      DISABLE_AUTH: "true"
    volumes:
      - type: "bind"
        source: "/Users/rahul/github/github.com/kaggle-lead" 
        target: "/home/rstudio"
      - type: "bind"
        source: "$LOCAL_RENV_CACHE"
        target: "/renv/cache"
